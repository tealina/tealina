{
  "scripts": {
    "dev": "concurrently -n \"Server Web\" \"pnpm -F server dev\" \"pnpm -F web dev\"",
    "init-demo": "pnpm -F server init-demo"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}