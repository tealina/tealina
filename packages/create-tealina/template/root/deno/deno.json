{
  "workspace": [
    "./packages/*"
  ],
  "tasks": {
    "frontend": "deno run --allow-net --allow-read ./packages/web/src/index.ts",
    "backend": "deno run -A ./packages/server/src/index.ts",
    "dev": "deno task frontend & deno task backend"
  }
}